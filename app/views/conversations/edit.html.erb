<%= form_for (@conversation), :url => { :action => "update" } do |f| %>

<div class="field">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>
<div class="actions">
  <%= f.submit "Submit" %>
</div>

<% end %>
